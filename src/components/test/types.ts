/**
 * Interface representing a user answer.
 */
export interface UserAnswer {
  schema_id: number
  answer_score: number
}

/**
 * Interface representing a schema.
 */
export interface Schema {
  id: number
  name: string
}
